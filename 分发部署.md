# ExcelAI — 分发与部署说明

## 方式 A：GitHub Pages + Microsoft 365 集中部署（推荐）

1. 运行 `node deploy.mjs https://bjzzxhzl.github.io/deepseek-excel-assistant`，脚本会同时更新云端 `manifest.xml` 与本机 `manifest-standalone.xml`。
2. 将 `taskpane.html`、`app.js`、`app.css`、四个 PNG 图标和 `manifest.xml` 推送到 GitHub 仓库 `main` 根目录；Pages 从 `main /(root)` 发布。
3. Microsoft 365 管理员在“设置 → 集成应用 → 上传自定义应用”中上传 `manifest.xml`，再分配给用户或组。
4. 完全退出并重启 Excel，点击“开始”选项卡中的 **ExcelAI**。

GitHub Pages 只托管静态页面，不保存 API Key，也不负责 Office 用户授权。

## 方式 B：Windows 本机兼容安装

1. 关闭所有 Excel 窗口。
2. 运行 `dist\ExcelAI-Standalone-Setup-v0.39.exe`。
3. 安装器使用独立的 `manifest-standalone.xml` 和本机加载项 ID，不会覆盖 Microsoft 365 集中部署版。
4. 若组织策略禁止 Developer 侧载，请改用方式 A；本机安装器不能绕过组织策略。

为兼容旧版原位升级，内部安装目录、注册键和启动载体文件名仍保留 `DeepSeekExcelAssistant` 标识，对外显示名称均为 ExcelAI。

## 方式 C：本地开发调试

1. 运行 `node deploy.mjs --local`，再启动 `node server.mjs`。
2. 运行 `安装注册.bat` 注册本机清单，完全重启 Excel。
3. 调试完成后，重新运行公网地址的 `deploy.mjs`，避免将 localhost 清单误用于发布。

## 安全说明

- API Key 只保存在使用者本机 Office WebView 的 `localStorage`，静态服务器不存储、不转发密钥。
- 写入、删除和格式修改等敏感操作默认请求批准；用户可在聊天工具栏切换权限模式。
- 公网部署必须使用 HTTPS；不要把 API Key、测试账号或其他密钥提交到 Git。
