# ExcelAI — 版本记录

## v0.40（动态模型能力与新对话启动）

- 切换 DeepSeek、SiliconFlow、OpenRouter 或自定义供应商时，实时请求该 API 的 `/models` 目录并同步到主界面；新增手动刷新、加载/失败提示和按供应商记忆 API Key 与模型
- 推理强度改为模型级动态选项：优先读取 `reasoning.supported_efforts` 等能力元数据；DeepSeek 使用 low/high/max，OpenRouter 使用统一 `reasoning.effort`，SiliconFlow 推理模型映射到 `thinking_budget`
- 补齐 OpenRouter 的 `reasoning` / `reasoning_details` 流式解析；无推理能力的模型自动禁用思考与强度控件，强制推理模型不再允许关闭
- 插件每次打开默认进入空白新对话，同时完整保留历史记录；修复动态模型在页面重载前无目录缓存时回退为供应商默认模型的问题
- 清单与静态资源升级为 `1.0.40.0` / v40；174 项自动化断言全部通过，并完成 OpenRouter 实时 381 模型、模型推理档位与 360px 窄屏 Playwright 验收

## v0.39（品牌升级、设置中心与统一发布）

- 插件对外名称统一升级为 **ExcelAI**；云端集中部署清单与 Windows 本机清单同步更新到 `1.0.39.0`，保留旧安装目录、注册键、加载项 ID 与载体文件名以支持原位升级
- 设置弹窗重构为 API、技能、快捷指令、外观、数据管理五类导航与卡片式布局，补齐窄任务窗格响应式样式、键盘 Esc/遮罩关闭和无障碍语义
- 完成自定义供应商未保存改动、切换丢失、重命名与重复名称校验等遗留 bug 修复；修复任务窗格缺失 favicon 的 404
- `deploy.mjs` 现在同时更新 `manifest.xml` 与 `manifest-standalone.xml`，并同步 SupportUrl/AppDomain；本机已安装目录会一起刷新页面、样式、脚本、图标和两套清单
- 项目目录正式接入 Git 与现有 GitHub Pages 仓库；缓存键升级为 v39，自动化回归 163 项全部通过，并完成 1280px / 360px Playwright 视觉与交互验收

## v0.38（双分发隔离、自定义供应商与 Markdown）

- 保留 Microsoft 365 管理员集中部署，同时生成 Windows EXE 本机兼容版；两套清单使用不同加载项 ID，EXE 的安装、升级与卸载不会撤销管理员分配
- 旧版 EXE 升级时自动迁移旧的本机 Developer 注册与隐藏启动载体，新版使用 `manifest-standalone.xml` 和 `DeepSeekExcelAssistant-Standalone-Autoload.xlsm`
- 自定义供应商支持手动命名、base URL、模型与持久化，可连续添加多个并在下拉框中切换；兼容迁移旧单一自定义地址
- 模型回复新增 Markdown H1–H6 标题、列表、引用、分隔线、表格、粗体、斜体、行内代码与代码块渲染，修复 `## 选区结构` 显示为原始文本
- 清单和静态资源缓存键升级为 1.0.38.0 / v38；自动化测试扩展到 148 项并全部通过

## v0.36（多 Excel 进程自动加载修复）

- 根因复现：第一个 Excel 进程以可写方式占用隐藏载体后，第二个独立 Excel 进程会出现“文件正在使用”对话框；取消后该进程没有激活 DeepSeek，只能再从开发人员加载项手动启动
- `OPEN/OPENn` 启动参数改为 `/r "载体路径"`，让每个独立 Excel 进程以只读方式自动载入同一载体，消除文件锁冲突
- 桌面快捷方式同步识别新的只读启动参数，并在补载当前 Excel 会话时使用 `/r`
- 清单与静态资源缓存键升级到 1.0.36.0 / v36

## v0.35（顶部极简化与部署机制纠正）

- 移除任务窗格左上角的品牌、版本和 Excel API 状态文字
- 将会话记录/收起操作移到左上角，改为与参考图一致的“左侧栏”线性 SVG 图标，保留键盘与屏幕阅读器标签
- 为 `taskpane.html` 引用的 `app.css` / `app.js` 增加 v35 缓存键，避免 Office WebView 继续使用旧顶部界面
- 纠正“本机 Developer 注册等于全局用户部署”的错误说明：生产级的所有普通工作簿可用性需要 Microsoft 365 集成应用/集中部署
- 桌面快捷方式在 Excel 已经运行时会显式打开已注册的隐藏载体，修复“安装前已启动 Excel”会话中的漏加载；HTTPS 模式仍不需后台服务

## v0.34（Pages 图标发布与强制缓存失效）

- 将简化后的“表格 + AI 星芒” 16/32/80 PNG 真正同步到 GitHub Pages，并逐文件校验线上 SHA-256
- 清单和任务窗格升级为 1.0.34.0 / v0.34，所有图标与页面 URL 使用全新 `v=34` 缓存键，避免 Excel 继续命中旧方形占位图
- 修复 `deploy.mjs` 仅替换图标主路径、却保留旧查询参数的问题；今后部署会统一重写页面和三个图标的版本参数
- 重建隐藏启动载体与 Inno Setup 安装包，安装/升级后会精确清理本加载项的功能区验证缓存

## v0.33（无需侧载工作簿的自动启动）

- 复现并确认共享文件夹“添加”在当前 Excel 环境中仍是文档级测试侧载，重启后不会进入“我的加载项”
- 新增 `DeepSeekExcelAssistant-Autoload.xlsm` 隐藏启动载体，安装到应用私有受信任目录并通过不冲突的 `OPEN/OPENn` 启动项加载；Excel 启动即出现 DeepSeek 功能区按钮
- 载体加入仅作用于自身的退出清洁事件，消除 Excel 因嵌入式任务窗格状态产生的重复保存提示
- 避免把整个 XLSTART 目录设为可信；安装与卸载会精确登记/移除专用载体启动项，不覆盖用户现有 Excel 启动配置
- 已验证三种场景：无可见工作簿时图标存在、新建普通工作簿自动可用、既有普通工作簿自动可用并能打开任务窗格
- 安装器改为当前用户、无管理员权限、无网络共享、无手工“添加”；卸载自动清理隐藏载体和清单映射
- 图标精简为单一“表格 + AI 星芒”符号，并重新导出 16/32/80 PNG 与多尺寸 ICO
- 清单、Pages 页面和任务窗格版本升级为 1.0.33.0 / v0.33

## v0.32（全工作簿持久安装 + 专业图标）

- 根因修复：停止把 `WEF\Developer` 或内嵌 WebExtension 的侧载工作簿当作正式安装；改为本机共享目录 + Office“共享文件夹”一次性添加
- 安装器补齐 TrustedCatalog 的 `Id` / `Url` / `Flags=1`，并通过管理员辅助程序创建只读本机共享
- `start-all.vbs` 不再打开 `deepseek-excel-assistant-sideload.xlsx`，只启动 Excel；安装包不再包含侧载工作簿
- 设计新的透明“表格 + AI 助手”图标，自动导出 16/32/80 PNG 与 16/32/48/64/256 ICO
- 清单、Pages 页面和任务窗格版本升级为 1.0.32.0 / v0.32

## v0.31（开始选项卡与开始菜单图标修复）
- 统一根目录与 `pages` 清单，按钮放到 Excel「开始」选项卡（`OfficeTab id="TabHome"`）
- 补齐基础图标、`AddinCommands 1.1` 需求声明和 `FunctionFile`，并为静态资源加入 v31 缓存破坏参数
- 安装/卸载只清理本加载项的 `CustomUIValidationCache`，避免旧清单持续占用功能区
- 安装模式改为当前用户侧载，避免 UAC 使用其他管理员账号时把 HKCU/安装目录写给错误用户
- 新增真正的多尺寸 `app.ico`，Windows 开始菜单和桌面快捷方式显式指定图标
- 修正侧载工作簿内嵌的加载项版本，开始菜单启动器改为打开该工作簿，使 DeepSeek 按钮每次都能注入当前 Excel 会话
- 公网 HTTPS 模式不再错误强制依赖 Node.js；只有清单指向 localhost 时才启动本地服务
- 修复「生成安装包.bat」的乱码与拼行，编译前自动重生成图标

## v0.30（功能区常驻·最终修正）
- 诊断确认：受信任目录注册生效（共享文件夹可见），但该 Excel 版本不渲染「开始」选项卡内嵌分组
- 改为**独立「DeepSeek」选项卡**（CustomTab，渲染优先级最高）+ 补写 Flags/单复数路径注册 + 清缓存
- 重新编译安装包（dist\DeepSeekExcelAssistant-Setup-v0.30.exe）

## v0.29（一键安装·免手动设置）
- 安装器升级为管理员权限：自动创建网络共享（share-helper）+ 写入受信任目录（UNC）+ 开发者注册
- 其他机器安装 = 双击安装包 + 一次 UAC + 重启 Excel，无需任何手动共享/对话框步骤；卸载自动删除共享
- 重新编译安装包（dist\DeepSeekExcelAssistant-Setup-v0.29.exe）

## v0.28b（免服务器部署方案）
- 新增 deploy.mjs（一键切换部署地址）与「部署到GitHubPages.bat」、pages 上传目录
- 支持 GitHub Pages 等 HTTPS 静态托管：彻底去掉本地服务器（无黑窗、无空闲退出、无需双击启动，兼容 Excel 网页版/Mac）

## v0.28（一体化启动 + 常驻方案修正）
- 新增 start-all.vbs 一体化启动器：双击一次 = 隐藏启动服务器 + 自动打开 Excel（消除手动两步）
- 移除本地路径目录注册（实测该 Excel 版本不生效）；改用官方"网络共享目录"常驻方案（net share + 受信任目录对话框，README 附一次性步骤）
- 重新编译安装包（dist\DeepSeekExcelAssistant-Setup-v0.28.exe）

## v0.27（功能区位置调整）
- 按用户偏好：按钮放回「开始」选项卡（TabHome 分组），不再使用独立选项卡
- 保留 TrustedCatalogs 目录注册（重启 Excel 后分组常驻）
- 新版文件已直接同步到已安装目录（免重装生效）；重新编译安装包（dist\DeepSeekExcelAssistant-Setup-v0.27.exe）

## v0.26（功能区常驻）
- 功能区改为**独立「DeepSeek」选项卡**（更醒目），不再依赖开始选项卡分组
- 写入「受信任的加载项目录」注册（TrustedCatalogs）：重启 Excel 后选项卡常驻，无需每次从加载项里启用
- 重新编译安装包（dist\DeepSeekExcelAssistant-Setup-v0.26.exe）

## v0.25（静默后台 + 资源节约）
- 服务器无窗口后台运行：新增 start-server.vbs 隐藏启动器，桌面/开始菜单快捷方式不再弹黑窗
- 资源节约：任务窗格打开期间每 2 分钟心跳保活；窗格关闭后 15 分钟无请求服务器自动退出（空闲检测实测通过）
- 重新编译安装包（dist\DeepSeekExcelAssistant-Setup-v0.25.exe）

## v0.24（日志折叠）
- 工具调用日志默认折叠：摘要行只显示「🔧 工具名」，点击展开查看完整参数与结果
- 重新编译安装包（dist\DeepSeekExcelAssistant-Setup-v0.24.exe）

## v0.23（修复 + 清理）
- 修复：用户主动发送对话后，空白区的快捷操作仍不消失
- 重新编译 Inno 安装包（dist\DeepSeekExcelAssistant-Setup-v0.23.exe）
- 清理本机 PoC 残留（注册表项与 poc 目录）

## v0.22（安装体验）
- 快捷操作移到新建对话的空白区展示（选择后消失），不再挤在任务栏
- 权限开关移到聊天工具栏（🔐 请求批准 / ⚡ 替我批准）
- 清单升级为功能区按钮版（开始选项卡 DeepSeek 分组）：点击按钮才打开任务窗格，普通打开工作簿不再自动弹出
- 生成 16/32/80 图标；安装包体系：**Inno Setup 安装器（setup.iss + 生成安装包.bat）**，可选安装路径、桌面/开始菜单快捷方式、卸载自动清理（dist\DeepSeekExcelAssistant-Setup-v0.22.exe 已编译，约 2MB）

## v0.21（第三阶段·存储安全）
- saveState 异常保护：存储空间不足时不再中断发送流程，状态栏提示导出备份
- 设置弹窗「数据管理」显示对话记录存储用量（浏览器上限约 5MB）

## v0.20（第三阶段·自定义）
- ⚡ 快捷指令可自定义（设置弹窗增删、持久化）
- Ctrl/Cmd+Enter 强制发送
- 会话列表 ✎ 显式重命名按钮

## v0.19（第三阶段·体验）
- 修复中文输入法组合中回车误发送（isComposing/keyCode 229 守卫）
- 阅读历史时不再被新消息强制拽到底部（近底才自动滚动）
- 模型输出的 Markdown 表格渲染为真表格

## v0.18（第三阶段·菜单补齐）
- set_page_setup 打印设置工具（打印区域/方向/缩放/按页适配）
- create_chart 图例开关
- 分发部署文档

## v0.17（第三阶段·会话搜索）
- 侧边栏会话搜索：按标题或消息内容实时过滤

## v0.16（第三阶段·快捷操作）
- 快捷指令按钮（📊分析选区/📈生成图表/🔢数据透视/🎨格式美化）
- 条件格式补齐 dataBar/iconSet
- 发送后显示"已附加：上下文 X KB · N 个文件"

## v0.15（透视表修复·最终）
- create_pivot 改用官方推荐的 hierarchies 集合获取与添加字段（修复 pivotFields undefined 导致的创建失败）
- 创建失败时列出已有透视表并提示换位置

## v0.14（透视表修复·二）
- 枚举已加载集合定位透视表 + 源区域表头降级读取 + 全链路守卫
- 复制按钮移到气泡右下角

## v0.13（透视表修复·一）
- read_range 通用区域读取工具（先看表头再创建）
- 工具描述与系统提示强化表头确认

## v0.12（加固）
- 透视表双重失败友好错误 + 🧰 工具清单帮助弹窗

## v0.11（用户反馈修复）
- 透视表两步式创建 + 字段名自动匹配 + 逐字段容错
- 每条消息 📋 复制按钮
- 🔐 权限设置（请求批准/替我批准）

## v0.10（菜单扩展·三）
- 清除内容/格式、隐藏行/列、工作表管理（新建/重命名/删除）
- 表格名清洗、排序/删图表参数守卫

## v0.9（菜单扩展·二）
- list_charts + 工具 Schema 合法性与名称唯一性校验

## v0.8（菜单扩展·一）
- set_size（行高列宽/自动调整）、merge_cells（合并拆分）

## v0.7（菜单扩展·核心 16 工具）
- 条件格式、图表、表格、排序、筛选、冻结窗格、数据验证、批注、迷你图、名称管理器、数据透视表 4 工具、数据连接刷新

## v0.5–v0.6（技能与侧载）
- 技能删除、导出/导入（JSON）；停止生成；一键安装/卸载脚本

## v0.3–v0.4（界面重构）
- API 设置收进右上角弹窗；聊天工具栏（模型/推理/上下文/技能/附件）；侧边栏折叠默认收起

## v0.1–v0.2（MVP 基础）
- DeepSeek V4 接入（思考模式 + reasoning_effort）、上下文三模式、多会话持久化、外观主题、文件上传、Agent 读写与确认
